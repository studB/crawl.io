---
phase: 04-cli-packaging
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - package.json
  - README.md
  - LICENSE
  - .gitattributes
autonomous: true
requirements: [CLI-04, CLI-05]

must_haves:
  truths:
    - "`package.json` declares a `bin` field of `{ \"crawl\": \"./dist/bin/crawl.js\" }` and the `files` allowlist is exactly `[\"dist/\", \"README.md\", \"LICENSE\"]` (CLI-04)"
    - "`package.json` declares `engines: { \"node\": \">=20\" }` and `license: \"MIT\"` (CLI-05)"
    - "Non-empty `description`, `keywords` (5 strings), and placeholder `repository` / `bugs` / `homepage` entries exist (CLI-05 publish readiness)"
    - "`README.md` exists at repo root and covers: install command, quick-start example with a sample markdown config, env vars (NAVER_ID, NAVER_PW, CRAWL_HEADED_TIMEOUT_MS), exit codes table"
    - "`LICENSE` exists at repo root containing the MIT license text with a copyright line"
    - "`npm pack --dry-run` succeeds AND its output lists `dist/`, `package.json`, `README.md`, `LICENSE`; it does NOT list `src/`, `test/`, `.planning/`, `node_modules/`, `vitest.config.ts`, `.planning` (CLI-05)"
  artifacts:
    - path: "package.json"
      provides: "Publish-ready metadata — bin, files, engines, keywords, license MIT, description, repository/bugs/homepage placeholders"
      contains: "\"bin\""
    - path: "README.md"
      provides: "User-facing docs — install, quick-start, env vars, exit codes"
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: ".gitattributes"
      provides: "Enforce LF line endings for .js / .ts / package.json so shebang survives on Windows author machines"
  key_links:
    - from: "package.json.bin.crawl"
      to: "dist/bin/crawl.js (emitted by Plan 04-01 Task 3)"
      via: "relative path ./dist/bin/crawl.js"
      pattern: "\"crawl\":\\s*\"\\./dist/bin/crawl\\.js\""
    - from: "package.json.files"
      to: "the tarball contents"
      via: "allowlist that INCLUDES dist/, README.md, LICENSE"
      pattern: "\"files\""
    - from: "README.md"
      to: "package.json (install command references the package name)"
      via: "`npm install -g crawl.io` example"
      pattern: "npm install"
---

<objective>
Finalize `package.json` publish metadata (bin, files, engines, keywords, license, description, repository/bugs/homepage placeholders), write a minimal but complete `README.md`, land the MIT `LICENSE`, and add a `.gitattributes` to enforce LF line endings for the bin file. Zero touches to CLI source — Plan 01 created all of src/cli and src/bin; this plan just makes the package publishable around them.

Purpose: Convert the repo from "CLI code exists" (Plan 01) to "publishable npm package" (CLI-04 + CLI-05). Plan 03 then validates the tarball end-to-end.

Output: Updated package.json, new README.md, new LICENSE, new .gitattributes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cli-packaging/04-CONTEXT.md
@CLAUDE.md
@package.json
@.gitignore
@.planning/phases/04-cli-packaging/04-01-SUMMARY.md

<interfaces>
<!-- The exact package.json edits Plan 02 must land. Plan 01's SUMMARY reports the
     commander version and the bin file path (./dist/bin/crawl.js) that Plan 01
     already built and verified. -->

Required package.json fields (adds/overrides on the existing object):

```json
{
  "name": "crawl.io",
  "version": "0.1.0",
  "description": "Markdown-configured Playwright web crawler — one .md file describes a crawl job and carries its own results back in the same file.",
  "keywords": ["crawler", "playwright", "naver", "markdown", "cli"],
  "license": "MIT",
  "author": "",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "crawl": "./dist/bin/crawl.js" },
  "engines": { "node": ">=20" },
  "files": ["dist/", "README.md", "LICENSE"],
  "repository": { "type": "git", "url": "https://github.com/TBD/crawl.io.git" },
  "bugs": { "url": "https://github.com/TBD/crawl.io/issues" },
  "homepage": "https://github.com/TBD/crawl.io#readme",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepack": "npm run build"
  },
  "dependencies": { /* keep existing + commander (added in Plan 01) */ },
  "devDependencies": { /* keep existing */ }
}
```

NOTE: the `repository`/`bugs`/`homepage` values use the literal placeholder host `github.com/TBD/crawl.io` — the user will edit to a real URL before `npm publish`. Tests assert the strings are NON-EMPTY and are NOT literally the empty string — they do not assert a specific URL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json metadata (bin, files, engines, keywords, license, description, scripts, repo placeholders)</name>
  <files>package.json</files>
  <read_first>
    - `package.json` (current state post-Plan-01 — commander is in deps, build script is already present)
    - `.planning/phases/04-cli-packaging/04-CONTEXT.md` §Package Publish Readiness (locks the exact fields and values)
    - `.planning/phases/04-cli-packaging/04-01-SUMMARY.md` (confirms dist/bin/crawl.js is the emitted path)
  </read_first>
  <action>
    Edit `package.json` to the shape shown in the `<interfaces>` block above. Specifically:

    1. **description**: set to `"Markdown-configured Playwright web crawler — one .md file describes a crawl job and carries its own results back in the same file."` (or equivalent one-sentence tagline — must be non-empty).

    2. **keywords**: `["crawler", "playwright", "naver", "markdown", "cli"]` (exactly 5 entries, exact strings from CONTEXT.md).

    3. **license**: change from `"ISC"` to `"MIT"`.

    4. **author**: leave as `""` — the user fills this in before publish. Must remain a string key (not removed).

    5. **bin**: add `"bin": { "crawl": "./dist/bin/crawl.js" }`.

    6. **engines**: add `"engines": { "node": ">=20" }`.

    7. **files**: change from `["dist"]` to `["dist/", "README.md", "LICENSE"]` (trailing slash on `dist/` is idiomatic; the other two files are created in Tasks 2–3 of this plan).

    8. **main** + **types**: verify still `"dist/index.js"` and `"dist/index.d.ts"` (no change from current state).

    9. **repository**, **bugs**, **homepage**: add placeholder entries using the TBD GitHub shape from the <interfaces> block. These are PLACEHOLDERS — tests assert non-empty strings, NOT specific URLs. The user overwrites before publish.

    10. **scripts**: add `"prepack": "npm run build"` — ensures `npm pack` always ships a fresh build. Leave existing scripts (`build`, `typecheck`, `test`, `test:watch`, and the postbuild shim if Plan 01 added one) intact.

    11. Keep **dependencies** and **devDependencies** exactly as Plan 01 left them — do NOT re-order or remove anything; commander must remain in dependencies.

    Format the file with 2-space indentation matching the current style. Preserve JSON validity (`node -e 'JSON.parse(require("fs").readFileSync("package.json","utf8"))'` exits 0).
  </action>
  <acceptance_criteria>
    - `node -e 'JSON.parse(require("fs").readFileSync("package.json","utf8"))'` exits 0 (valid JSON)
    - `node -e 'const p=require("./package.json"); process.exit(p.license==="MIT"?0:1)'` exits 0
    - `node -e 'const p=require("./package.json"); process.exit(p.bin?.crawl==="./dist/bin/crawl.js"?0:1)'` exits 0
    - `node -e 'const p=require("./package.json"); process.exit(p.engines?.node===">=20"?0:1)'` exits 0
    - `node -e 'const p=require("./package.json"); const f=p.files; process.exit(Array.isArray(f)&&f.length===3&&f.includes("dist/")&&f.includes("README.md")&&f.includes("LICENSE")?0:1)'` exits 0
    - `node -e 'const p=require("./package.json"); const k=p.keywords; process.exit(Array.isArray(k)&&k.length===5&&["crawler","playwright","naver","markdown","cli"].every(x=>k.includes(x))?0:1)'` exits 0
    - `node -e 'const p=require("./package.json"); process.exit(typeof p.description==="string"&&p.description.length>20?0:1)'` exits 0
    - `node -e 'const p=require("./package.json"); process.exit(p.repository?.url && p.bugs?.url && p.homepage ? 0 : 1)'` exits 0 (placeholders present, non-empty)
    - `node -e 'const p=require("./package.json"); process.exit(p.scripts?.prepack==="npm run build"?0:1)'` exits 0
    - `node -e 'const p=require("./package.json"); process.exit(p.dependencies?.commander && p.dependencies?.playwright ? 0 : 1)'` exits 0 (commander + playwright both still in deps)
    - `node -e 'const p=require("./package.json"); process.exit(p.devDependencies?.commander?1:0)'` exits 0 (commander NOT duplicated in devDeps)
    - `npm run build` exits 0 (the prepack script uses this; verify it still works)
    - `test -x dist/bin/crawl.js && [ "$(head -1 dist/bin/crawl.js)" = "#!/usr/bin/env node" ]` (Plan 01 invariants preserved)
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const p=require("./package.json"); const errs=[]; if(p.license!=="MIT")errs.push("license"); if(p.bin?.crawl!=="./dist/bin/crawl.js")errs.push("bin"); if(p.engines?.node!==">=20")errs.push("engines"); if(!(Array.isArray(p.files)&&p.files.length===3&&p.files.includes("dist/")&&p.files.includes("README.md")&&p.files.includes("LICENSE")))errs.push("files"); if(!(p.keywords?.length===5&&["crawler","playwright","naver","markdown","cli"].every(x=>p.keywords.includes(x))))errs.push("keywords"); if(!(p.description?.length>20))errs.push("description"); if(!(p.repository?.url && p.bugs?.url && p.homepage))errs.push("repo placeholders"); if(p.scripts?.prepack!=="npm run build")errs.push("prepack"); if(!p.dependencies?.commander)errs.push("commander dep"); if(p.devDependencies?.commander)errs.push("commander dup"); if(errs.length){console.error("MISSING:",errs.join(",")); process.exit(1);} process.exit(0);' && npm run build</automated>
  </verify>
  <done>package.json has bin, files, engines, keywords (5), license MIT, description, repository/bugs/homepage placeholders, prepack script; build still produces executable shebang-preserved dist/bin/crawl.js; JSON valid.</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with install + quick-start + env vars + exit codes</name>
  <files>README.md</files>
  <read_first>
    - `.planning/phases/04-cli-packaging/04-CONTEXT.md` §Package Publish Readiness README bullet (what sections are required)
    - `.planning/phases/04-cli-packaging/04-01-SUMMARY.md` (exit-code mapping, --help flags, --verbose/--quiet behavior — mirror it accurately)
    - `CLAUDE.md` §Project (core value statement to reuse in the README intro)
    - `.planning/REQUIREMENTS.md` §OUT (for the exit-code wording)
    - `src/auth/*.ts` existing env var references — ensure README env-var table lists the same names (`NAVER_ID`, `NAVER_PW`, `CRAWL_HEADED_TIMEOUT_MS`)
  </read_first>
  <action>
    Create `README.md` at repo root. The file MUST contain the following sections (use H2 or deeper headings; exact wording is Claude's discretion but must include the listed anchors):

    1. **Title + tagline**: `# crawl.io` followed by a one-sentence description.

    2. **What it does** (~2–3 sentences): `One markdown file fully describes a crawl job and carries its own results. Point the tool at a .md file, the tool runs Playwright against the declared URL, extracts the named selectors, and writes the results back into the same file as a timestamped Output section.`

    3. **Install** section — heading must contain the word `install`. Must include the code block:
       ```bash
       npm install -g crawl.io
       ```

    4. **Quick start** section — heading contains `quick start` or `usage`. Must include:
       - A fenced code block showing a minimal sample `job.md` with `# URL`, `# Selectors` (YAML), and `# Rules` (YAML). The selectors block must include at least one field and demonstrate either the `engine: css` default or an explicit value; the rules block must include `waitFor` and `timeout`. Example:
         ```markdown
         # URL
         https://example.com/

         # Selectors
         ```yaml
         title:
           selector: h1
         ```

         # Rules
         ```yaml
         waitFor: h1
         timeout: 30000
         ```
         ```
       - A fenced `bash` block showing `crawl run job.md`.
       - A note that the tool will create / append to a `# Output` section in the same file on each run.

    5. **Environment variables** section — heading contains `env` (case-insensitive). Must include a table or bullet list with:
       - `NAVER_ID` — Naver login ID (required when crawling Naver Cafe pages)
       - `NAVER_PW` — Naver login password (required when crawling Naver Cafe pages)
       - `CRAWL_HEADED_TIMEOUT_MS` — captcha / 2FA resolve wait (default 300000)

    6. **Exit codes** section — heading contains `exit` (case-insensitive). Must include `0` (success) and `1` (any failure) with one-line descriptions. Wording must align with OUT-05 ("non-zero exit on any failure").

    7. **CLI help reference** section OR a sentence inside Quick Start mentioning `crawl --help` and `crawl run --help`.

    8. **Status / version** footer (optional) mentioning v1 scope: single-page, markdown-config, v1-only single Naver Cafe flow. Keep to ≤2 lines.

    Keep the whole file under ~150 lines. No deep-dive docs — Phase 4 CONTEXT locks `README` as minimal.
  </action>
  <acceptance_criteria>
    - `test -f README.md` passes
    - `wc -l README.md` reports a line count > 30 (substantive) and < 200 (not bloated)
    - `grep -qi '^#.*crawl\\.io' README.md` passes (title present)
    - `grep -qi 'npm install -g crawl\\.io' README.md` passes (install command)
    - `grep -qi 'crawl run' README.md` passes (usage example)
    - `grep -qE '^#.*[Ss]elector' README.md || grep -qE '# Selectors' README.md` passes (sample config references the selectors section)
    - `grep -q 'NAVER_ID' README.md` passes
    - `grep -q 'NAVER_PW' README.md` passes
    - `grep -q 'CRAWL_HEADED_TIMEOUT_MS' README.md` passes
    - `grep -qE '\\b0\\b' README.md && grep -qE '\\b1\\b' README.md` passes (exit codes mentioned)
    - `grep -qiE '(exit codes?|non-zero)' README.md` passes (exit-codes section present)
    - `grep -qE '\`\`\`(bash|markdown|yaml)' README.md` passes (at least one fenced code block present)
    - `grep -qi 'crawl --help' README.md || grep -qi 'crawl run --help' README.md` passes
    - `git diff --name-only -- src/ dist/` → empty (README doesn't touch code or build)
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md && grep -qi 'npm install -g crawl\.io' README.md && grep -q 'NAVER_ID' README.md && grep -q 'NAVER_PW' README.md && grep -q 'CRAWL_HEADED_TIMEOUT_MS' README.md && grep -qiE '(exit codes?|non-zero)' README.md && grep -qE '```' README.md && [ "$(wc -l < README.md)" -gt 30 ] && [ "$(wc -l < README.md)" -lt 200 ]</automated>
  </verify>
  <done>README.md at repo root with install, quick-start (with YAML sample config), env vars table, exit codes section, and --help pointer; 30–200 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Create LICENSE (MIT) + .gitattributes (LF enforcement)</name>
  <files>LICENSE, .gitattributes</files>
  <read_first>
    - `.planning/phases/04-cli-packaging/04-CONTEXT.md` §Package Publish Readiness (LICENSE + line-endings note)
    - `package.json` (for the year / author placeholder — the license copyright line should say `Copyright (c) 2026 crawl.io contributors` or similar — author is empty by design)
  </read_first>
  <action>
    1. Create `LICENSE` at repo root with the standard MIT license text, including:
       - First line: `MIT License`
       - Blank line
       - `Copyright (c) 2026 crawl.io contributors` (year is 2026 per the current date from CLAUDE.md; author name is deliberately generic since `package.json.author` is empty)
       - Blank line
       - The standard MIT body beginning `Permission is hereby granted, free of charge, to any person obtaining a copy...` and ending with the `THE SOFTWARE IS PROVIDED "AS IS"...` warranty disclaimer.

       Use the canonical MIT text from opensource.org/licenses/MIT. Preserve standard line breaks.

    2. Create `.gitattributes` at repo root with LF enforcement for files where line endings matter to downstream users (primarily the compiled shebang script). Minimal content:
       ```
       * text=auto eol=lf
       *.ts text eol=lf
       *.js text eol=lf
       *.json text eol=lf
       *.md text eol=lf
       dist/bin/crawl.js text eol=lf
       ```

       Rationale: if a contributor on Windows clones and runs `npm run build`, the dist bin file's shebang line must NOT have CRLF — `#!/usr/bin/env node\r` breaks as a shell interpreter directive. The explicit `dist/bin/crawl.js` line documents intent even though the generic `*.js` rule covers it.
  </action>
  <acceptance_criteria>
    - `test -f LICENSE` passes
    - `head -1 LICENSE` equals `MIT License`
    - `grep -q 'Copyright (c) 2026' LICENSE` passes
    - `grep -q 'Permission is hereby granted' LICENSE` passes (body present)
    - `grep -q 'THE SOFTWARE IS PROVIDED' LICENSE` passes (warranty clause present)
    - `[ "$(wc -l < LICENSE)" -gt 15 ]` (standard MIT is ~21 lines; reject 1-line stubs)
    - `test -f .gitattributes` passes
    - `grep -q 'eol=lf' .gitattributes` passes
    - `grep -q 'dist/bin/crawl.js' .gitattributes` passes (bin file LF lock explicit)
    - `npm run build` still exits 0 (no regression from adding .gitattributes)
  </acceptance_criteria>
  <verify>
    <automated>test -f LICENSE && [ "$(head -1 LICENSE)" = "MIT License" ] && grep -q 'Copyright (c) 2026' LICENSE && grep -q 'Permission is hereby granted' LICENSE && grep -q 'THE SOFTWARE IS PROVIDED' LICENSE && [ "$(wc -l < LICENSE)" -gt 15 ] && test -f .gitattributes && grep -q 'eol=lf' .gitattributes && grep -q 'dist/bin/crawl\.js' .gitattributes</automated>
  </verify>
  <done>LICENSE file with MIT text and 2026 copyright; .gitattributes enforces LF for text files and explicitly for dist/bin/crawl.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → npm registry | Metadata consumed by registry + `npm install` clients |
| `files` allowlist → tarball | Whitelist decides what ships publicly |
| shebang line → exec loader | Line ending on `#!/usr/bin/env node` determines POSIX exec behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | `files` allowlist leak | mitigate | `files: ["dist/", "README.md", "LICENSE"]` excludes src/, test/, .planning/, and node_modules. Plan 03 Task 1 asserts via `npm pack --dry-run` that none of the excluded paths appear in the listed tarball |
| T-04-06 | Tampering | CRLF in shebang breaks bin on POSIX | mitigate | `.gitattributes` enforces `eol=lf` globally and for `dist/bin/crawl.js` explicitly; Plan 03 Task 1 asserts the packed bin still has `#!/usr/bin/env node` as the first line |
| T-04-07 | Information Disclosure | Repository placeholder leaks author metadata | accept | `repository/bugs/homepage` use the literal `github.com/TBD/crawl.io` placeholder; user is explicitly instructed to overwrite before publish; no PII leaked pre-publish |
| T-04-08 | Repudiation | License mis-claim | mitigate | LICENSE file contains full MIT text with copyright line; package.json.license === "MIT" — SPDX registry consumers get a single unambiguous signal |
</threat_model>

<verification>
- `npm run build` exits 0, dist/bin/crawl.js still executable with shebang
- `node -e 'JSON.parse(require("fs").readFileSync("package.json","utf8"))'` exits 0
- All acceptance_criteria node -e assertions from Task 1 pass
- `test -f README.md && test -f LICENSE && test -f .gitattributes`
- No edits under src/ or dist/ (verify with `git diff --name-only -- src/ dist/` → empty)
</verification>

<success_criteria>
- package.json is publish-ready: bin, files, engines, keywords, license MIT, description, repo/bugs/homepage placeholders, prepack script — CLI-04, CLI-05
- README.md covers install, usage, env vars, exit codes — CLI-05 publish readiness
- LICENSE contains full MIT text with 2026 copyright — CLI-05 publish readiness
- .gitattributes enforces LF for text files — T-04-06 mitigation
- No regression: Plan 01's dist/bin/crawl.js invariants (executable + shebang) still hold
- No edits to src/ or library code
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-packaging/04-02-SUMMARY.md` covering: package.json diff, README structure (section headings present), LICENSE author line chosen, any .gitattributes decisions.
</output>
