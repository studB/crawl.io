---
phase: 04-cli-packaging
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - test/cli/cli.integration.test.ts
  - test/cli/pack.integration.test.ts
  - test/fixtures/cli/minimal-public.md
  - vitest.config.ts
autonomous: true
requirements: [CLI-01, CLI-03, CLI-04, CLI-05]

must_haves:
  truths:
    - "CLI integration tests spawn the built dist/bin/crawl.js via child_process.spawn and assert crawl --help stdout contains the word run and the project tagline (CLI-03)"
    - "CLI integration tests assert crawl run --help stdout contains both NAVER_ID and NAVER_PW string literals (CLI-03)"
    - "CLI integration tests assert crawl run <nonexistent>.md exits 1 and stderr contains 'config not found:' with a scrubbed path (OUT-05 + CONTEXT.md Specific Ideas)"
    - "CLI integration test asserts crawl run <valid-public-config>.md exits 0 and stdout contains a success summary line (gated by network availability; soft-skip when offline)"
    - "CLI integration test asserts crawl run --quiet <nonexistent>.md writes ZERO bytes to stdout AND ZERO bytes to stderr AND still exits 1"
    - "npm pack --dry-run succeeds in a subprocess and its JSON output lists files for dist/index.js, dist/bin/crawl.js, README.md, LICENSE and NO entries under src/, test/, .planning/, or node_modules/"
    - "Actual npm pack produces a .tgz; extracting it and running node <extracted>/dist/bin/crawl.js --help exits 0 with the same help text (CLI-04)"
    - "npm publish --dry-run exits 0 (CLI-05)"
  artifacts:
    - path: "test/cli/cli.integration.test.ts"
      provides: "CLI --help / run --help / pre-flight / quiet-flag integration tests via child_process.spawn against dist/bin/crawl.js"
    - path: "test/cli/pack.integration.test.ts"
      provides: "npm pack --dry-run + tarball-extract smoke test + npm publish --dry-run integration tests"
    - path: "test/fixtures/cli/minimal-public.md"
      provides: "Minimal valid markdown config pointing at a stable public URL (https://example.com) for the happy-path CLI test"
    - path: "vitest.config.ts"
      provides: "Include path extended so test/**/*.integration.test.ts is discovered"
  key_links:
    - from: "test/cli/cli.integration.test.ts"
      to: "dist/bin/crawl.js"
      via: "child_process.spawn(process.execPath, ['dist/bin/crawl.js', ...args])"
      pattern: "dist/bin/crawl\\.js"
    - from: "test/cli/pack.integration.test.ts"
      to: "npm pack / npm publish --dry-run"
      via: "child_process.spawn('npm', ['pack', '--dry-run', '--json'])"
      pattern: "npm.*pack.*--dry-run"
    - from: "vitest.config.ts include pattern"
      to: "test/cli/*.integration.test.ts"
      via: "include array addition"
      pattern: "test/.+integration\\.test\\.ts"
---

<objective>
Validate the Phase 4 CLI + packaging end-to-end. Run integration tests against the built dist/bin/crawl.js via child_process.spawn to prove help text, pre-flight, quiet flag, and exit codes behave as specified. Run npm pack --dry-run to prove tarball contents are correct. Run npm publish --dry-run as the final publish-readiness gate.

Purpose: This plan CONFIRMS the Phase 4 goal — "invokable after npm install -g, with help text, npm-publishable." Plans 01-02 BUILT the pieces; Plan 03 proves they compose into a shippable package.

Output: Two new integration test files under test/cli/, one markdown fixture, and a vitest.config.ts include-path update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cli-packaging/04-CONTEXT.md
@.planning/phases/04-cli-packaging/04-01-SUMMARY.md
@.planning/phases/04-cli-packaging/04-02-SUMMARY.md
@CLAUDE.md
@package.json
@vitest.config.ts
@src/crawler/runner.integration.test.ts

<interfaces>
<!-- child_process.spawn helper shape the CLI integration tests share. Use this verbatim. -->

```typescript
import { spawn } from 'node:child_process';
import { resolve as resolvePath } from 'node:path';

const BIN = resolvePath(process.cwd(), 'dist/bin/crawl.js');

interface RunResult { code: number | null; stdout: string; stderr: string; }

async function runBin(args: readonly string[], opts?: { timeoutMs?: number; cwd?: string; env?: NodeJS.ProcessEnv }): Promise<RunResult> {
  return new Promise<RunResult>((resolve, reject) => {
    const child = spawn(process.execPath, [BIN, ...args], {
      cwd: opts?.cwd ?? process.cwd(),
      env: opts?.env ?? { ...process.env },
      stdio: ['ignore', 'pipe', 'pipe'],
    });
    let stdout = '', stderr = '';
    child.stdout.on('data', (c) => { stdout += c.toString(); });
    child.stderr.on('data', (c) => { stderr += c.toString(); });
    const timer = setTimeout(() => child.kill('SIGKILL'), opts?.timeoutMs ?? 60_000);
    child.on('close', (code) => { clearTimeout(timer); resolve({ code, stdout, stderr }); });
    child.on('error', reject);
  });
}
```

## npm pack --dry-run --json output shape

`npm pack --dry-run --json` writes a JSON array to stdout, one element per tarball:

```json
[{
  "id": "crawl.io@0.1.0",
  "name": "crawl.io",
  "version": "0.1.0",
  "files": [ { "path": "dist/index.js", "size": N, "mode": 420 }, ... ],
  "entryCount": N,
  "bundled": []
}]
```

Tests parse this JSON and iterate over `files[].path` to assert inclusions/exclusions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture config + vitest include update</name>
  <files>test/fixtures/cli/minimal-public.md, vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (current include: ['src/**/*.test.ts', 'src/**/*.integration.test.ts'] — must be extended)
    - src/crawler/runner.integration.test.ts (existing integration test reference)
    - .planning/phases/02-core-crawler-output/02-02-SUMMARY.md or 02-03-SUMMARY.md for the canonical markdown config shape
    - src/config/schema.ts for the minimum valid fixture content
  </read_first>
  <action>
    1. Create `test/fixtures/cli/minimal-public.md` — valid-per-Phase-1 markdown config pointing at a stable public URL. Content (with real triple-backticks, NOT escaped):

       Section 1: `# URL` header followed by a blank line then `https://example.com/`.
       Section 2: `# Selectors` header followed by a fenced ```yaml code block containing exactly one field: `title:` with child `selector: h1`.
       Section 3: `# Rules` header followed by a fenced ```yaml code block containing `waitFor: h1` and `timeout: 30000`.
       No `# Output` section — runCrawl creates it on first run.

       `https://example.com/` is the canonical stable page for smoke tests; the `<h1>` text is "Example Domain" (no network hand-shake required beyond DNS + TLS + one GET).

    2. Update `vitest.config.ts` `include` to discover the new test location. Extend the array from:
       ```
       include: ['src/**/*.test.ts', 'src/**/*.integration.test.ts'],
       ```
       to:
       ```
       include: [
         'src/**/*.test.ts',
         'src/**/*.integration.test.ts',
         'test/**/*.integration.test.ts',
       ],
       ```
       Do NOT touch other fields (environment, setupFiles, testTimeout, hookTimeout, passWithNoTests).

    3. Rebuild and verify nothing regressed:
       ```bash
       npm run build
       npx vitest run
       ```
       The pre-existing suite still passes the 181 + 4-skip baseline from Plan 03-03 SUMMARY. The new test files don't exist yet (Tasks 2-3 create them); `passWithNoTests: true` covers the interim.

    4. Verify the fixture PARSES semantically (not just as a string) by round-tripping through the Phase 1 parser:
       ```bash
       node -e "const {parseConfigFile}=require('./dist/index.js'); parseConfigFile('test/fixtures/cli/minimal-public.md').then(j=>{if(!j.url||!j.selectors?.title||!j.rules?.timeout)process.exit(2);process.exit(0)}).catch(e=>{console.error(e.message);process.exit(1)})"
       ```
       Exit 0 required.
  </action>
  <acceptance_criteria>
    - `test -f test/fixtures/cli/minimal-public.md` passes
    - `grep -q '# URL' test/fixtures/cli/minimal-public.md` passes
    - `grep -q 'https://example.com' test/fixtures/cli/minimal-public.md` passes
    - `grep -q '# Selectors' test/fixtures/cli/minimal-public.md` passes
    - `grep -q '# Rules' test/fixtures/cli/minimal-public.md` passes
    - `grep -q 'waitFor:' test/fixtures/cli/minimal-public.md` passes
    - `grep -q 'timeout:' test/fixtures/cli/minimal-public.md` passes
    - `grep -qE "test/\*\*/\*\.integration\.test\.ts" vitest.config.ts` passes
    - Fixture semantic-parses via the Phase 1 parser (the node -e one-liner in step 4) → exit 0
    - `npx vitest run` exits 0 (no regression)
    - `git diff --name-only -- src/` → empty (no source edits)
  </acceptance_criteria>
  <verify>
    <automated>test -f test/fixtures/cli/minimal-public.md && grep -q '# URL' test/fixtures/cli/minimal-public.md && grep -q 'https://example.com' test/fixtures/cli/minimal-public.md && grep -q 'waitFor:' test/fixtures/cli/minimal-public.md && grep -qE "test/\*\*/\*\.integration\.test\.ts" vitest.config.ts && npm run build && node -e "const {parseConfigFile}=require('./dist/index.js'); parseConfigFile('test/fixtures/cli/minimal-public.md').then(j=>{if(!j.url||!j.selectors?.title||!j.rules?.timeout)process.exit(2);process.exit(0)}).catch(e=>{console.error(e.message);process.exit(1)})" && npx vitest run</automated>
  </verify>
  <done>Fixture file valid and parseable; vitest include extended; pre-existing suite still passes; no source edits.</done>
</task>

<task type="auto">
  <name>Task 2: Build test/cli/cli.integration.test.ts — spawn dist/bin/crawl.js and assert help + pre-flight + quiet</name>
  <files>test/cli/cli.integration.test.ts</files>
  <read_first>
    - test/fixtures/cli/minimal-public.md (from Task 1)
    - .planning/phases/04-cli-packaging/04-01-SUMMARY.md (what --help / run --help actually print per Plan 01 Task 3 smoke)
    - .planning/phases/04-cli-packaging/04-CONTEXT.md Specific Ideas (config-not-found message, --quiet suppresses both streams)
    - src/crawler/output.ts scrubPaths (for understanding the expected stderr path format on non-existent)
    - src/crawler/runner.integration.test.ts (setup pattern for mkdtemp fixture paths)
  </read_first>
  <action>
    Create `test/cli/cli.integration.test.ts` with the runBin helper from the <interfaces> block AND the following test suite:

    1. **beforeAll hook:** assert `dist/bin/crawl.js` exists and has the shebang. If missing, run `npm run build` inline (or fail loudly with a message pointing at Plan 04-01). Rationale: vitest runs from repo root; Plan 01 built dist, Plan 02 left it intact, but a fresh clone may not have built yet — the test guards itself.

    2. **Test: `crawl --help` prints top-level usage**
       - Run `runBin(['--help'])`.
       - Assert `code === 0`.
       - Assert `stdout` matches `/Usage: crawl/i` (commander default prefix).
       - Assert `stdout` contains the string `run` (the subcommand is listed).
       - Assert `stdout` contains either `Markdown-configured` OR the literal tagline substring from package.json description.
       - Assert `stderr` is empty.

    3. **Test: `crawl run --help` prints run-subcommand usage with env vars and exit codes**
       - Run `runBin(['run', '--help'])`.
       - Assert `code === 0`.
       - Assert `stdout` contains the literal strings `NAVER_ID` and `NAVER_PW`.
       - Assert `stdout` contains `CRAWL_HEADED_TIMEOUT_MS`.
       - Assert `stdout` contains BOTH the literal character `0` and `1` near the word `exit` (case-insensitive). Use a regex like `/exit(\s+code)?s?[\s\S]{0,200}\b0\b[\s\S]{0,200}\b1\b/i` OR two separate assertions: `/exit/i.test(stdout)` AND `/\b0\b/.test(stdout)` AND `/\b1\b/.test(stdout)`.
       - Assert `stdout` includes a usage line mentioning `<file>` (the positional arg).
       - Assert `stderr` is empty.

    4. **Test: `crawl run <nonexistent>.md` exits 1 with scrubbed stderr and never launches Chromium**
       - Compose `const missing = '/tmp/gsd-plan04-test-missing-' + process.pid + '-' + Date.now() + '.md'`.
       - Run `runBin(['run', missing], { timeoutMs: 10_000 })` (short timeout — no browser, should return instantly).
       - Assert `code === 1`.
       - Assert `stderr` contains `config not found:`.
       - Assert `stderr` contains the basename (or a suffix of the path) — but does NOT necessarily contain the full absolute `/tmp/...` if scrubPaths handled it. Use a loose check: `stderr` contains `gsd-plan04-test-missing-` (substring survives scrubbing).
       - Assert `stdout` is empty.
       - Assert neither stream contains `chromium`, `Playwright`, or `launching` (no browser path was taken).

    5. **Test: `crawl run --quiet <nonexistent>.md` suppresses both streams, still exits 1**
       - Run `runBin(['run', '--quiet', '/tmp/gsd-plan04-quiet-missing-'+process.pid+'.md'], { timeoutMs: 10_000 })`.
       - Assert `code === 1`.
       - Assert `stdout === ''`.
       - Assert `stderr === ''`.

    6. **Test: `crawl run -v <nonexistent>.md` (verbose) writes at least one stderr progress line**
       - Run `runBin(['run', '-v', '/tmp/gsd-plan04-verbose-missing-'+process.pid+'.md'], { timeoutMs: 10_000 })`.
       - Assert `code === 1`.
       - Assert `stderr.length > 0`.
       - Assert `stderr` contains BOTH `config not found:` AND an arrow hint `→` OR the word `parsing` (from Plan 01 Task 2 §verbose). (Either of those is acceptable evidence that --verbose added progress context.)

    7. **Test: `crawl run <valid-public-fixture>.md` — HAPPY PATH, network-gated**
       - Compose `const fixture = resolvePath(process.cwd(), 'test/fixtures/cli/minimal-public.md')`.
       - Copy the fixture to a tempdir (`fs.mkdtempSync(path.join(os.tmpdir(), 'crawl-cli-')) + '/job.md'`) so the run's Output writeback doesn't dirty the repo.
       - Run `runBin(['run', tempPath], { timeoutMs: 60_000 })`.
       - This test is gated by `RUN_CLI_NETWORK_TESTS === '1'` env var. Use `const GATED = process.env.RUN_CLI_NETWORK_TESTS === '1';` and wrap the test in `describe.skipIf(!GATED)` (mirror Phase 3 gated-integration pattern). Provide a sibling `describe.skipIf(GATED)` sentinel block with a single `it.skip('CLI network test skipped — set RUN_CLI_NETWORK_TESTS=1 to run')`.
       - When GATED: assert `code === 0`. Assert `stdout` starts with `✓` OR contains `title` (the sample fixture's only field). Assert the temp file now contains `# Output` after the run (proving runCrawl round-tripped).
       - Clean up tempdir in afterEach.

    8. Use `describe('crawl CLI integration', () => { ... })` at the outer level. Use `it(...)` for each test above. Use `expect(...)` from vitest.

    9. TypeScript strict compliance: all imports use `node:` protocol (`node:child_process`, `node:path`, `node:os`, `node:fs/promises`); test-only `any` is forbidden — lean on `RunResult` interface.

    10. Run the full suite:
        ```bash
        npm run build
        npx vitest run test/cli/cli.integration.test.ts
        ```
        The 5 always-on tests pass; the happy-path test is skipped by default (gate closed) with a single informative skip line.
  </action>
  <acceptance_criteria>
    - `test -f test/cli/cli.integration.test.ts` passes
    - `grep -q "describe('crawl CLI integration'" test/cli/cli.integration.test.ts` passes (or `describe(\"crawl CLI integration\"`)
    - `grep -q "spawn" test/cli/cli.integration.test.ts` passes
    - `grep -q "dist/bin/crawl\\.js" test/cli/cli.integration.test.ts` passes
    - `grep -q "NAVER_ID" test/cli/cli.integration.test.ts` passes
    - `grep -q "NAVER_PW" test/cli/cli.integration.test.ts` passes
    - `grep -q "config not found:" test/cli/cli.integration.test.ts` passes
    - `grep -q "describe.skipIf" test/cli/cli.integration.test.ts` passes (gated happy-path test)
    - `grep -q "RUN_CLI_NETWORK_TESTS" test/cli/cli.integration.test.ts` passes
    - `! grep -q ": any" test/cli/cli.integration.test.ts` passes (no escape hatch)
    - `npm run build && npx vitest run test/cli/cli.integration.test.ts` exits 0; 5 tests pass (or 5 pass + 1 sentinel-skip) with gate CLOSED
    - `npx tsc --noEmit -p tsconfig.json` still exits 0 (test files included in project)
    - `git diff --name-only -- src/` → empty (no source edits)
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q "NAVER_ID" test/cli/cli.integration.test.ts && grep -q "NAVER_PW" test/cli/cli.integration.test.ts && grep -q "config not found:" test/cli/cli.integration.test.ts && grep -q "describe.skipIf" test/cli/cli.integration.test.ts && grep -q "dist/bin/crawl\.js" test/cli/cli.integration.test.ts && ! grep -q ": any" test/cli/cli.integration.test.ts && npx tsc --noEmit -p tsconfig.json && npx vitest run test/cli/cli.integration.test.ts</automated>
  </verify>
  <done>5 CLI behavior integration tests spawn the built binary and assert help, pre-flight, quiet, verbose; a sixth network-gated happy-path test is skipped by default; tsc clean; library untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Build test/cli/pack.integration.test.ts — npm pack --dry-run + tarball extract smoke + npm publish --dry-run</name>
  <files>test/cli/pack.integration.test.ts</files>
  <read_first>
    - .planning/phases/04-cli-packaging/04-02-SUMMARY.md (package.json final shape — bin, files, engines, etc.)
    - package.json (to compare with pack output)
    - .planning/phases/04-cli-packaging/04-CONTEXT.md Package Publish Readiness bullet (exact publish verification sequence)
    - npm docs: npm pack --dry-run --json output shape (see <interfaces> block in this plan)
  </read_first>
  <action>
    Create `test/cli/pack.integration.test.ts` with three top-level tests:

    1. **Test: `npm pack --dry-run --json` lists the correct tarball contents**
       - Use child_process.spawn to run `npm pack --dry-run --json` with cwd=repo root.
       - Buffer stdout, wait for close, assert exit code 0.
       - Parse stdout as JSON. Expect an array of length 1. Grab the first element.
       - Expected INCLUSIONS (each must appear in `result[0].files[].path`): `dist/index.js`, `dist/index.d.ts`, `dist/bin/crawl.js`, `dist/cli/cli.js`, `dist/cli/run.js`, `dist/cli/exit.js`, `package.json`, `README.md`, `LICENSE`.
       - Expected EXCLUSIONS (none of these may appear in any `files[].path`): any path starting with `src/`, any path starting with `test/`, any path starting with `.planning/`, any path starting with `node_modules/`, `vitest.config.ts`, `tsconfig.json`, `.gitignore`, `.gitattributes`.
       - Assert each inclusion/exclusion explicitly (individual expect calls so failures point at the exact missing / extra entry).
       - Assert `result[0].name === 'crawl.io'`.
       - Timeout: 60_000 ms (npm pack prepack runs the build; first cold build on WSL can be slow).
       - Before running the test, ensure the repo is built (add a prebuild step in beforeAll: `npm run build` if `dist/bin/crawl.js` is missing).

    2. **Test: Actual `npm pack` produces a .tgz; extracted bin `--help` works**
       - Use `fs.mkdtempSync` to create a temp workspace.
       - Run `npm pack --pack-destination=<tmp>` (spawn, 60s timeout). Assert exit code 0. Stdout contains the tarball filename (e.g., `crawl.io-0.1.0.tgz`).
       - Extract the tarball: use `tar -xzf <tmp>/crawl.io-0.1.0.tgz -C <tmp>` (spawn `tar`, 10s timeout). Assert exit 0.
       - After extraction, the content is under `<tmp>/package/`. Assert `<tmp>/package/dist/bin/crawl.js` exists.
       - Assert `head -1 <tmp>/package/dist/bin/crawl.js` equals `#!/usr/bin/env node` (shebang survived tarball round-trip).
       - Spawn `node <tmp>/package/dist/bin/crawl.js --help`; assert exit 0 and stdout contains `run` (smoke test).
       - Clean up tempdir in afterAll.
       - Gate via env: skip when `SKIP_PACK_TESTS === '1'` (escape hatch for constrained CI). Default is to run (the test must pass in the local verification loop).

    3. **Test: `npm publish --dry-run` exits 0**
       - Spawn `npm publish --dry-run` (60s timeout) with cwd=repo root and `stdio: ['ignore', 'pipe', 'pipe']`.
       - Assert exit code 0.
       - Assert stdout or stderr contains the package name `crawl.io@0.1.0` AND the word `tarball` OR `filename` (evidence npm ran the full publish pipeline).
       - Explicit assertion that no tarball was actually uploaded: this is `--dry-run`, so the command prints the manifest and exits without hitting the registry. No further network mock is needed.
       - This is the FINAL PUBLISH-READINESS GATE for CLI-05.

    4. TypeScript strict compliance — no `any`; use a typed spawn helper.

    5. Run the suite:
       ```bash
       npm run build
       npx vitest run test/cli/pack.integration.test.ts
       ```
       All 3 tests pass.
  </action>
  <acceptance_criteria>
    - `test -f test/cli/pack.integration.test.ts` passes
    - `grep -q "npm pack --dry-run --json" test/cli/pack.integration.test.ts || grep -q "'pack', '--dry-run', '--json'" test/cli/pack.integration.test.ts` passes
    - `grep -q "npm publish --dry-run" test/cli/pack.integration.test.ts || grep -q "'publish', '--dry-run'" test/cli/pack.integration.test.ts` passes
    - `grep -q "dist/bin/crawl\\.js" test/cli/pack.integration.test.ts` passes
    - `grep -q "README.md" test/cli/pack.integration.test.ts && grep -q "LICENSE" test/cli/pack.integration.test.ts` passes
    - `grep -q "src/" test/cli/pack.integration.test.ts && grep -q "node_modules" test/cli/pack.integration.test.ts` passes (asserts exclusions)
    - `grep -q "#!/usr/bin/env node" test/cli/pack.integration.test.ts` passes (shebang round-trip assertion)
    - `grep -q "mkdtempSync\\|mkdtemp" test/cli/pack.integration.test.ts` passes (uses a throwaway dir)
    - `! grep -q ": any" test/cli/pack.integration.test.ts` passes
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `npx vitest run test/cli/pack.integration.test.ts` exits 0 (3 tests pass)
    - `npm pack --dry-run --json | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8")); const files=j[0].files.map(f=>f.path); const want=["dist/bin/crawl.js","package.json","README.md","LICENSE"]; const bad=["src/index.ts","test/cli/cli.integration.test.ts","node_modules","vitest.config.ts"]; const missing=want.filter(x=>!files.includes(x)); const leaks=bad.filter(x=>files.some(f=>f===x||f.startsWith(x+"/")||f.startsWith(x))); if(missing.length){console.error("missing",missing);process.exit(1)} if(leaks.length){console.error("leaks",leaks);process.exit(1)} process.exit(0)'` exits 0 (standalone acceptance of pack contents)
    - `npm publish --dry-run` exits 0 (standalone final gate)
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q "publish.*--dry-run\|'publish', '--dry-run'" test/cli/pack.integration.test.ts && grep -q "pack.*--dry-run\|'pack', '--dry-run'" test/cli/pack.integration.test.ts && grep -q "#!/usr/bin/env node" test/cli/pack.integration.test.ts && ! grep -q ": any" test/cli/pack.integration.test.ts && npx tsc --noEmit -p tsconfig.json && npx vitest run test/cli/pack.integration.test.ts && npm pack --dry-run --json > /tmp/gsd-pack-$$.json && node -e 'const j=JSON.parse(require("fs").readFileSync("/tmp/gsd-pack-'"$$"'.json","utf8")); const files=j[0].files.map(f=>f.path); const want=["dist/bin/crawl.js","package.json","README.md","LICENSE"]; const bad=["src/index.ts","test/cli/cli.integration.test.ts","node_modules","vitest.config.ts"]; const missing=want.filter(x=>!files.includes(x)); const leaks=bad.filter(x=>files.some(f=>f===x||f.startsWith(x+"/"))); if(missing.length){console.error("missing",missing);process.exit(1)} if(leaks.length){console.error("leaks",leaks);process.exit(1)} process.exit(0)' && npm publish --dry-run > /dev/null 2>&1; rm -f /tmp/gsd-pack-$$.json</automated>
  </verify>
  <done>Three pack/publish integration tests pass; standalone `npm pack --dry-run --json` contents match the allowlist; standalone `npm publish --dry-run` exits 0. Phase 4 goal proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess (npm/tar/node dist/bin) → test process | Test spawns subprocesses; output is captured and asserted, not executed |
| tarball → filesystem | `tar -xzf` into mkdtemp-ed dir — contained, cleaned up in afterAll |
| `npm publish --dry-run` → registry | `--dry-run` guarantees NO network push; test never exposes credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | tarball accidentally includes .env / session / planning docs | mitigate | Task 3 Test 1 explicitly asserts `src/`, `test/`, `.planning/`, `node_modules/`, `vitest.config.ts` are absent from pack JSON. If the `files` allowlist regresses, this test fails before publish |
| T-04-10 | Tampering | Real `npm publish` triggered accidentally by test | mitigate | Tests only invoke `npm publish --dry-run` — the `--dry-run` flag is checked grep-wise in acceptance criteria; no test spawns `npm publish` without the flag |
| T-04-11 | Denial of Service | Test hangs on subprocess that never exits | mitigate | Every spawn has an explicit `setTimeout(kill('SIGKILL'), timeoutMs)` wrapper; test-level timeouts 60s (npm pack) / 10s (tar + --help). Vitest testTimeout=60000 is the outer net |
| T-04-12 | Spoofing | Tarball shebang stripped between build and pack | mitigate | Task 3 Test 2 asserts `head -1 <extracted>/package/dist/bin/crawl.js === '#!/usr/bin/env node'` AFTER the npm pack + tar round-trip — the only way this passes is if the whole pipeline (tsc → postbuild → .gitattributes → npm pack → tar) preserves LF and the first line |
| T-04-13 | Information Disclosure | Temp paths leak into test failure messages | accept | os.tmpdir() paths in assertions are low-value (generic `/tmp/...` prefix on Linux, `%TEMP%` on Windows); no secret material involved |
</threat_model>

<verification>
- `npx vitest run` — full suite including new test/cli/*.integration.test.ts — exits 0 (181+ existing + 5 new CLI + 3 new pack = ~189+ active, gated tests still skipped)
- `npx vitest run test/cli/` alone exits 0
- `npm pack --dry-run --json` output meets the allowlist (standalone acceptance in Task 3)
- `npm publish --dry-run` exits 0 (standalone acceptance in Task 3)
- `head -1 dist/bin/crawl.js` === `#!/usr/bin/env node` (Plan 01 invariant preserved)
- `head -1` of the extracted-tarball bin === `#!/usr/bin/env node` (Task 3 Test 2)
- `npx tsc --noEmit -p tsconfig.json` exit 0
- `git diff --name-only -- src/` → empty
- `git diff --name-only -- src/config src/crawler src/auth src/index.ts` → empty (library untouched across Plans 01–03)
</verification>

<success_criteria>
- 5 CLI behavior tests spawn dist/bin/crawl.js and assert help text (NAVER_ID/NAVER_PW present), pre-flight error (scrubbed `config not found:`, exit 1), --quiet double-suppress, --verbose progress — CLI-01, CLI-03
- 1 gated network test validates happy-path CLI against example.com when RUN_CLI_NETWORK_TESTS=1 — CLI-01
- npm pack --dry-run --json test asserts tarball includes dist/, README.md, LICENSE, package.json AND excludes src/, test/, .planning/, node_modules/ — CLI-04
- Tarball extraction + shebang round-trip test proves the bin survives npm pack — CLI-04
- npm publish --dry-run test is the final publish-readiness gate — CLI-05
- All three phase-goal success criteria verified: `crawl --help` + `crawl run --help` present usage with env vars; subcommand structure extensible; npm pack tarball has working bin; npm publish --dry-run succeeds
- Library byte-unchanged throughout Phase 4 (src/config, src/crawler, src/auth, src/index.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-packaging/04-03-SUMMARY.md` covering: test counts added (5 CLI + 3 pack + 1 gated = 9 new), final baseline total, any surprises in npm pack output (paths present/absent), whether `npm publish --dry-run` surfaced any warnings that should be documented, and whether Phase 4 ROADMAP criteria 1-4 are all verified green.
</output>
