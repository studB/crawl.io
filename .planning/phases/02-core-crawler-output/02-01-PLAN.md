---
phase: 02-core-crawler-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - src/crawler/types.ts
  - src/crawler/errors.ts
  - src/crawler/errors.test.ts
autonomous: true
requirements: [CRWL-01]
tags: [playwright, typescript, types, errors, install]

must_haves:
  truths:
    - "playwright@^1 is a runtime dependency of crawl.io"
    - "Chromium browser binary is installed so `npx playwright install chromium` finishes successfully on a clean clone"
    - "CrawlErrorCode is a string-literal union with exactly: 'timeout' | 'selector_miss' | 'network' | 'frame_not_found' | 'extraction_failed' | 'config_parse' | 'unknown'"
    - "CrawlError extends Error and exposes readonly { code: CrawlErrorCode, detail?: string }"
    - "CrawlResult is the exported envelope type with required fields { status, configPath, url, startedAt, durationMs } and optional fields { fields?, error? }"
  artifacts:
    - path: "src/crawler/types.ts"
      provides: "CrawlErrorCode union + CrawlResult envelope type"
      exports: ["CrawlErrorCode", "CrawlResult"]
    - path: "src/crawler/errors.ts"
      provides: "CrawlError class"
      exports: ["CrawlError"]
    - path: "src/crawler/errors.test.ts"
      provides: "Unit tests for CrawlError construction and code/detail contract"
    - path: "package.json"
      provides: "playwright runtime dep + optional postinstall hint"
      contains: "\"playwright\""
    - path: ".gitignore"
      provides: "ignore entry for .crawl-session.json (future Phase 3) and test-results / .playwright-artifacts"
      contains: ".crawl-session.json"
  key_links:
    - from: "src/crawler/errors.ts"
      to: "src/crawler/types.ts"
      via: "import type { CrawlErrorCode }"
      pattern: "import .* CrawlErrorCode"
    - from: "src/crawler/errors.test.ts"
      to: "src/crawler/errors.ts"
      via: "import { CrawlError }"
      pattern: "from ['\\\"]\\./errors['\\\"]"
---

<objective>
Install Playwright (library + Chromium browser binary) and create the crawler-layer type contracts every other Phase 2 plan imports from. No Playwright code is written here — only the types (`CrawlErrorCode`, `CrawlResult`) and the `CrawlError` class with a unit test.

Purpose: Plan 02 (output writeback) and Plan 03 (browser/frame/extract) both depend on `CrawlErrorCode` / `CrawlError` / `CrawlResult`. Landing them in Wave 1 lets Plans 02 and 03 run in parallel in Wave 2.

Output: `playwright` runtime dep + Chromium binary on disk; `src/crawler/types.ts` with the union + envelope; `src/crawler/errors.ts` with the `CrawlError` class; `src/crawler/errors.test.ts` with at least 5 passing tests; `.gitignore` extended for Phase 3 (`.crawl-session.json`) and Playwright artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-crawler-output/02-CONTEXT.md
@.planning/phases/01-config-parser/01-03-SUMMARY.md
@package.json
@tsconfig.json
@src/config/errors.ts
@src/config/types.ts
@src/config/index.ts
@src/index.ts

<interfaces>
<!-- Phase 1 API surface Phase 2 imports from. Do NOT re-declare these. -->

From src/config/types.ts:
```ts
export interface SelectorSpec {
  selector: string;
  engine: 'css' | 'xpath';
  frame?: string[];
}

export interface CrawlJob {
  url: string;
  selectors: Record<string, SelectorSpec>;
  rules: {
    waitFor?: string;
    timeout: number;
  };
}
```

From src/config/errors.ts (style reference — mirror the `declare readonly ... ?` trick to stay compatible with exactOptionalPropertyTypes + useDefineForClassFields):
```ts
export class ConfigParseError extends Error {
  readonly issues: string[];
  declare readonly filePath?: string;

  constructor(issues: string[], opts?: ConfigParseErrorOptions) {
    super(issues.join('\n'));
    this.name = 'ConfigParseError';
    this.issues = issues;
    if (opts?.filePath !== undefined) {
      this.filePath = opts.filePath;
    }
    Object.setPrototypeOf(this, ConfigParseError.prototype);
  }
}
```

From src/index.ts (current state — Phase 2 Plan 04 will extend, NOT this plan):
```ts
export type { CrawlJob, SelectorSpec, ConfigParseErrorOptions } from './config/index';
export { ConfigParseError, parseConfig, parseConfigFile } from './config/index';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright runtime + Chromium binary + extend .gitignore</name>

  <read_first>
    - package.json (current deps: remark-parse, unified, yaml, zod — Phase 1 only; no playwright)
    - package-lock.json (lockfileVersion 3 — npm install will update in place)
    - .gitignore (confirm contents before adding)
    - tsconfig.json (types:["node"] is present; no @types/playwright needed — playwright ships its own .d.ts)
    - .planning/phases/02-core-crawler-output/02-CONTEXT.md (Browser lifecycle section, Phase 3 storageState hook)
  </read_first>

  <files>package.json, package-lock.json, .gitignore</files>

  <action>
    Install Playwright as a runtime dependency and download the Chromium browser binary so subsequent plans can launch Chromium without further setup.

    1. Run `npm install --save playwright@^1` (exact range `^1.47.0` acceptable — any 1.x release is fine; `@playwright/test` is NOT needed, we use vitest for tests). This must update BOTH package.json (`dependencies.playwright`) and package-lock.json.

    2. Run `npx playwright install chromium` (no `--with-deps` — user is on WSL/Linux and we do not need root apt-get). This downloads the Chromium browser binary into `~/.cache/ms-playwright/chromium-*`. This MUST succeed; if it fails with a network error, retry once. If it still fails, surface the error and stop (do NOT mark the plan done — downstream plans need the binary).

    3. Append these entries to `.gitignore` (create the file if it does not exist — current state has NO `.gitignore` per `ls -la` of the repo root; double-check before writing):
       ```
       # Playwright session state (Phase 3)
       .crawl-session.json

       # Playwright test artifacts
       test-results/
       playwright-report/
       .playwright-artifacts/
       ```
       Use a single `>>` append if `.gitignore` already exists and already lists `node_modules`/`dist`; otherwise write the full file with the standard Node ignores (`node_modules/`, `dist/`, `*.log`, `.env`, `.env.*`) PLUS the block above.

    4. Do NOT add a `postinstall` script to package.json in this plan — it would make `npm install` for end users download Chromium automatically, which is the right UX but is a Phase 4 (packaging) concern. A TODO comment is NOT needed; Phase 4 will handle it.

    5. Verify: `require('playwright').chromium` must resolve at runtime, and `require('playwright').chromium.executablePath()` must return a non-empty string pointing at an existing binary.
  </action>

  <verify>
    <automated>
      node -e "const p = require('playwright'); if (typeof p.chromium?.launch !== 'function') process.exit(1); const ep = p.chromium.executablePath(); if (!ep || !require('fs').existsSync(ep)) { console.error('Chromium binary missing at', ep); process.exit(2); } console.log('OK playwright+chromium');"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -qE '"playwright"\s*:\s*"\^?1\.' package.json` exits 0 (exact: the key is `"playwright"` and the version starts with `^1.` or `1.`)
    - `grep -q '"playwright"' package-lock.json` exits 0
    - `test -f .gitignore` exits 0
    - `grep -q '^\.crawl-session\.json$' .gitignore` exits 0 (exact line: `.crawl-session.json`)
    - `grep -q '^test-results/$' .gitignore` exits 0
    - `node -e "require('playwright')"` exits 0 (no ERR_MODULE_NOT_FOUND, no ERR_REQUIRE_ESM)
    - `node -e "const fs=require('fs'); const p=require('playwright').chromium.executablePath(); process.exit(fs.existsSync(p)?0:1)"` exits 0 (Chromium binary actually on disk)
    - `npm test` still exits 0 (Phase 1 suite unchanged — 56/56 must still pass)
    - `npx tsc --noEmit` exits 0 (no type errors from the new dep)
    - `grep -q '"postinstall"' package.json` returns no match (we deliberately did NOT add one in this plan)
  </acceptance_criteria>

  <done>
    playwright@^1.x is in `dependencies`; Chromium binary is on disk at `chromium.executablePath()`; `.gitignore` lists `.crawl-session.json` + Playwright artifacts; the Phase 1 test suite is still green; no new TypeScript errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CrawlErrorCode + CrawlResult types, CrawlError class, and unit tests</name>

  <read_first>
    - src/config/errors.ts (mirror the `declare readonly ... ?` + conditional-assignment pattern for `detail?`)
    - src/config/errors.test.ts (mirror the test style — catch-and-introspect, assert instanceof, assert `.code` / `.detail` directly)
    - src/config/types.ts (module style — plain `export interface`, no default export)
    - tsconfig.json (noUncheckedIndexedAccess + exactOptionalPropertyTypes are ON — every array access must be guarded; optional fields use the declare-readonly trick)
    - .planning/phases/02-core-crawler-output/02-CONTEXT.md (Crawler Module & API decisions)
  </read_first>

  <behavior>
    - Test 1: `new CrawlError('timeout', 'waitFor #post failed after 30000ms')` produces an instance where `err instanceof CrawlError` is true, `err instanceof Error` is true, `err.name === 'CrawlError'`, `err.code === 'timeout'`, `err.detail === 'waitFor #post failed after 30000ms'`, and `err.message` contains both the code and the detail (format: `[timeout] waitFor #post failed after 30000ms`).
    - Test 2: `new CrawlError('network')` (no detail) produces an instance where `err.code === 'network'`, `'detail' in err === false` (exactOptionalPropertyTypes — the property must NOT exist on the instance when detail is omitted), and `err.message === '[network]'` (no trailing space, no trailing colon).
    - Test 3: Each of the 7 `CrawlErrorCode` literals (`'timeout'`, `'selector_miss'`, `'network'`, `'frame_not_found'`, `'extraction_failed'`, `'config_parse'`, `'unknown'`) can be used to construct a CrawlError without a TypeScript-level error and round-trips as `err.code`.
    - Test 4: `CrawlError.prototype` is correctly set so `err instanceof CrawlError` stays `true` after a JSON-round-trip-style re-hydration is NOT required (we do not support re-hydration in v1), BUT `Object.setPrototypeOf` fix is in place — mimic the errors.test.ts assertion.
    - Test 5: The `code` and `detail` fields are `readonly` at the TypeScript level. Assert this via a `// @ts-expect-error` line in the test file: `// @ts-expect-error code is readonly` followed by `err.code = 'network'`. The file MUST still typecheck (tsc --noEmit) because @ts-expect-error consumes the error.
  </behavior>

  <files>src/crawler/types.ts, src/crawler/errors.ts, src/crawler/errors.test.ts</files>

  <action>
    RED phase first. Create `src/crawler/errors.test.ts` with the 5 behaviors above, importing from `./errors` and `./types`. Run `npx vitest run src/crawler/errors.test.ts` — it MUST fail with "Cannot find module './errors'" (the expected RED signal). Commit as `test(02-01): add failing tests for CrawlError`.

    GREEN phase. Create the two source files:

    `src/crawler/types.ts` — EXACT content:
    ```ts
    export type CrawlErrorCode =
      | 'timeout'
      | 'selector_miss'
      | 'network'
      | 'frame_not_found'
      | 'extraction_failed'
      | 'config_parse'
      | 'unknown';

    export interface CrawlResult {
      status: 'ok' | 'error';
      configPath: string;
      url: string;
      startedAt: Date;
      durationMs: number;
      fields?: Record<string, string>;
      error?: {
        code: CrawlErrorCode;
        message: string;
      };
    }
    ```
    Rationale for the exact shape: locked by 02-CONTEXT.md. Do NOT add `stack?` to `error` — the CONTEXT note "stack?" on error objects refers to the JSON output shape, which is built in Plan 02 (output.ts), NOT the TypeScript `CrawlResult.error` interface. Keep `error.message` as the only user-facing string here.

    `src/crawler/errors.ts` — follow the `src/config/errors.ts` pattern:
    ```ts
    import type { CrawlErrorCode } from './types';

    export class CrawlError extends Error {
      readonly code: CrawlErrorCode;
      declare readonly detail?: string;

      constructor(code: CrawlErrorCode, detail?: string) {
        super(detail !== undefined ? `[${code}] ${detail}` : `[${code}]`);
        this.name = 'CrawlError';
        this.code = code;
        if (detail !== undefined) {
          this.detail = detail;
        }
        Object.setPrototypeOf(this, CrawlError.prototype);
      }
    }
    ```

    Run `npx vitest run src/crawler/errors.test.ts` — MUST pass 5/5. Commit as `feat(02-01): implement CrawlError + CrawlErrorCode + CrawlResult`.

    Do NOT export from `src/crawler/index.ts` yet (Plan 04 creates the barrel). Do NOT modify `src/index.ts` (Plan 04's job).
  </action>

  <verify>
    <automated>npx vitest run src/crawler/errors.test.ts && npx tsc --noEmit && grep -qE "^export type CrawlErrorCode = $|^export type CrawlErrorCode =$" src/crawler/types.ts && grep -q "'config_parse'" src/crawler/types.ts && grep -q "'frame_not_found'" src/crawler/types.ts && grep -q "declare readonly detail" src/crawler/errors.ts</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/crawler/types.ts` exits 0
    - `test -f src/crawler/errors.ts` exits 0
    - `test -f src/crawler/errors.test.ts` exits 0
    - `grep -cE "^\s+\| '(timeout|selector_miss|network|frame_not_found|extraction_failed|config_parse|unknown)'$" src/crawler/types.ts` returns exactly `7`
    - `grep -qE "export interface CrawlResult" src/crawler/types.ts` exits 0
    - `grep -qE "status: 'ok' \| 'error'" src/crawler/types.ts` exits 0
    - `grep -qE "fields\?: Record<string, string>" src/crawler/types.ts` exits 0
    - `grep -qE "error\?: \{" src/crawler/types.ts` exits 0 (error key on CrawlResult is optional)
    - `grep -qE "export class CrawlError extends Error" src/crawler/errors.ts` exits 0
    - `grep -q "declare readonly detail" src/crawler/errors.ts` exits 0 (exactOptionalPropertyTypes trick present)
    - `grep -q "Object.setPrototypeOf(this, CrawlError.prototype)" src/crawler/errors.ts` exits 0
    - `grep -cE "^\s*it\(" src/crawler/errors.test.ts` returns at least `5`
    - `grep -q "@ts-expect-error" src/crawler/errors.test.ts` exits 0 (readonly assertion test)
    - `npx vitest run src/crawler/errors.test.ts` exits 0 with all tests passing
    - `npx vitest run` exits 0 (full suite — 56 previous + 5+ new = 61+ passing)
    - `npx tsc --noEmit` exits 0
    - `grep -q "process.exit" src/crawler/errors.ts` returns no match (the class must NOT call process.exit — runCrawl's composability rule)
    - `grep -q "import.*playwright" src/crawler/errors.ts src/crawler/types.ts` returns no match (types layer stays free of Playwright imports — types are consumed by output.ts which is pure)
    - Git log shows TWO commits for Task 2 in order: `test(02-01)` RED then `feat(02-01)` GREEN (TDD gate)
  </acceptance_criteria>

  <done>
    `src/crawler/types.ts` exports `CrawlErrorCode` (7-member union) and `CrawlResult` (envelope with required + optional fields). `src/crawler/errors.ts` exports a `CrawlError` class with `code` (readonly) and optional `detail` (readonly, exactOptionalPropertyTypes-compliant). Tests pass. Full suite green. No Playwright import in the types layer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local | `npm install playwright` downloads code and a binary; supply-chain risk. |
| Playwright browser binary → filesystem | Chromium binary is downloaded from playwright.azureedge.net; integrity is verified by Playwright's installer via SHA checksums. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm install of playwright | accept | Pin via package-lock.json (lockfileVersion 3 is already in place); future `npm ci` uses the locked resolved URL + integrity hash. |
| T-02-02 | Information Disclosure | `.crawl-session.json` (Phase 3 artifact) accidentally committed | mitigate | Add `.crawl-session.json` to `.gitignore` in this plan (Task 1) — one phase before it can be created — so it is impossible to ever stage. |
| T-02-03 | Information Disclosure | Test artifacts / screenshots containing page content | mitigate | `.gitignore` includes `test-results/`, `playwright-report/`, `.playwright-artifacts/`. |
</threat_model>

<verification>
- `npm install` resolves cleanly from the lockfile (Task 1).
- `npx playwright install chromium` finished successfully (Task 1 manual step — binary presence verified by automated check).
- Full test suite passes: Phase 1's 56 tests + Task 2's new errors tests (5+).
- `npx tsc --noEmit` clean under strict + exactOptionalPropertyTypes + noUncheckedIndexedAccess.
- No file outside this plan's `files_modified` is touched.
</verification>

<success_criteria>
- [ ] `playwright@^1.x` present in `package.json` `dependencies` and `package-lock.json`
- [ ] `chromium.executablePath()` returns an existing file on disk
- [ ] `.gitignore` includes `.crawl-session.json` and Playwright artifact directories
- [ ] `src/crawler/types.ts` exports exactly the 7-member `CrawlErrorCode` and the `CrawlResult` envelope
- [ ] `src/crawler/errors.ts` exports `CrawlError` with readonly code, optional readonly detail, correct prototype chain
- [ ] `src/crawler/errors.test.ts` covers 5 behaviors; all pass
- [ ] Phase 1 suite (56 tests) still green
- [ ] `npx tsc --noEmit` clean
- [ ] Two commits for Task 2: RED (test) then GREEN (feat)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-crawler-output/02-01-SUMMARY.md` documenting:

(a) Confirmation that `playwright` is a runtime dep (not devDependencies) and that Chromium binary is on disk, including the output of `node -e "console.log(require('playwright').chromium.executablePath())"`.

(b) Exact contents of `src/crawler/types.ts` (copy-paste — short file, worth locking into the record).

(c) Exact contents of `src/crawler/errors.ts` (copy-paste).

(d) Test count and pass rate from `src/crawler/errors.test.ts` (expected: 5+, all passing).

(e) `.gitignore` delta (the lines added in this plan, so Phase 3 can grep-confirm `.crawl-session.json` is ignored before it writes the file).

(f) Requirement mapping: this plan addresses CRWL-01 (the "launches Playwright Chromium" precondition — installs the library and binary; the launch code itself lives in Plan 03's browser.ts).
</output>
