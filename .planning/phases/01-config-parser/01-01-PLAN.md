---
phase: 01-config-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - .gitignore
  - src/config/.gitkeep
  - src/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `npx tsc --noEmit` succeeds on an empty project with Node 20 + CommonJS emit"
    - "`unified`, `remark-parse`, `yaml`, `zod`, `vitest`, `@types/node`, `typescript` are installed at the versions resolved by npm at planning time"
    - "`npm run build` produces CommonJS output under `dist/` with `.d.ts` files"
    - "`npm test` invokes Vitest (even if zero tests exist yet) and exits 0"
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest with prod/dev deps and build/test scripts"
      contains: "\"unified\""
    - path: "tsconfig.json"
      provides: "TypeScript build configuration"
      contains: "\"rootDir\""
    - path: "src/config"
      provides: "Directory placeholder for parser module"
  key_links:
    - from: "package.json"
      to: "tsconfig.json"
      via: "build script `tsc -p tsconfig.json`"
      pattern: "\"build\":\\s*\"tsc"
    - from: "package.json"
      to: "node_modules"
      via: "dependencies field lists unified/remark-parse/yaml/zod"
      pattern: "\"zod\""
---

<objective>
Install the Phase 1 dependency set, lock the TypeScript build pipeline to CommonJS output, and create the project directory skeleton. This plan installs no code — only the toolchain needed for Plans 02 and 03 to compile.

Purpose: Greenfield repo has zero dependencies. Plans 02/03 cannot import `unified`, `remark-parse`, `yaml`, `zod`, or `vitest` until they're installed. Also, current `tsconfig.json` lacks `rootDir`/`outDir` and the package is `"type": "commonjs"` with `module: nodenext` — the emit target must be explicitly aligned so compiled JS actually runs.

Output: A buildable, testable empty project. `npm run build` and `npm test` both succeed. `src/config/` exists and is ready to be populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-config-parser/01-CONTEXT.md
@CLAUDE.md
@tsconfig.json
@package.json

<interfaces>
<!-- This plan creates no interfaces. It configures toolchain only. -->
<!-- Subsequent plans depend on: unified, remark-parse, yaml, zod, vitest, @types/node. -->
<!-- tsconfig invariants that MUST remain true (from CLAUDE.md): -->
<!--   strict: true, noUncheckedIndexedAccess: true, exactOptionalPropertyTypes: true -->
<!-- Package type invariant: "type": "commonjs" (keep — simpler npm publish for v1). -->
<!-- TS emit must therefore be CommonJS. With module: nodenext + type: commonjs, -->
<!-- nodenext will emit CJS automatically. Do NOT flip to ESM in v1. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 1 dependencies via npm</name>
  <read_first>
    - /home/ubuntu/work/crawl.io/package.json
    - /home/ubuntu/work/crawl.io/CLAUDE.md
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
From the repo root, run the following commands (do NOT hand-edit package.json to add dependency lines; use npm so package-lock.json stays coherent):

```
npm install --save unified remark-parse yaml zod
npm install --save-dev typescript vitest @types/node
```

Do not pass explicit version pins — accept whatever npm resolves at install time. Versions will be recorded into package-lock.json automatically.

After install, verify the resulting `package.json` has a `dependencies` object containing exactly: `unified`, `remark-parse`, `yaml`, `zod` (no extras), and a `devDependencies` object containing exactly: `typescript`, `vitest`, `@types/node`. If npm adds transitive deps to the top level, remove them.

Do NOT change `"type": "commonjs"` — it stays commonjs for v1 per the locked decision on npm-publish simplicity. Do NOT change `"main"` in this task (next task will).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=Object.keys(p.dependencies||{}).sort().join(','); const dd=Object.keys(p.devDependencies||{}).sort().join(','); if(d!=='remark-parse,unified,yaml,zod') throw new Error('deps mismatch: '+d); if(dd!=='@types/node,typescript,vitest') throw new Error('devDeps mismatch: '+dd); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` field contains exactly the keys `unified`, `remark-parse`, `yaml`, `zod` (verifiable: `node -e "console.log(Object.keys(require('./package.json').dependencies).sort())"` prints `[ 'remark-parse', 'unified', 'yaml', 'zod' ]`)
    - `package.json` `devDependencies` field contains exactly the keys `typescript`, `vitest`, `@types/node` (verifiable: same one-liner against `devDependencies`)
    - `package.json` retains `"type": "commonjs"` (verifiable: `node -e "console.log(require('./package.json').type)"` prints `commonjs`)
    - `node_modules/zod/package.json` exists (verifiable: `test -f node_modules/zod/package.json`)
    - `node_modules/unified/package.json` exists (verifiable: `test -f node_modules/unified/package.json`)
    - `node_modules/vitest/package.json` exists (verifiable: `test -f node_modules/vitest/package.json`)
    - `package-lock.json` exists and is non-empty (verifiable: `test -s package-lock.json`)
  </acceptance_criteria>
  <done>
    All four prod deps and three dev deps are installed, lockfile updated, `"type": "commonjs"` preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align tsconfig.json and wire build/test scripts</name>
  <read_first>
    - /home/ubuntu/work/crawl.io/tsconfig.json
    - /home/ubuntu/work/crawl.io/package.json
    - /home/ubuntu/work/crawl.io/CLAUDE.md
  </read_first>
  <files>tsconfig.json, package.json, .gitignore</files>
  <action>
1. Edit `tsconfig.json` — preserve every existing flag (strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, module: nodenext, target: esnext, verbatimModuleSyntax, isolatedModules, noUncheckedSideEffectImports, moduleDetection: force, skipLibCheck, declaration, declarationMap, sourceMap). Change or add only the following:
   - Set `"rootDir": "./src"`
   - Set `"outDir": "./dist"`
   - Set `"lib": ["esnext"]`
   - Set `"types": ["node"]` (replace the empty array)
   - Add `"include": ["src/**/*"]` at the top level (sibling to `compilerOptions`)
   - Add `"exclude": ["dist", "node_modules"]` at the top level
   - Remove the `"jsx": "react-jsx"` line (no JSX in this project; it can only confuse tooling)

2. Edit `package.json`:
   - Change `"main"` from `"index.js"` to `"dist/index.js"`
   - Add `"types": "dist/index.d.ts"`
   - Add `"files": ["dist"]`
   - Replace `"scripts"` with:
     ```json
     {
       "build": "tsc -p tsconfig.json",
       "typecheck": "tsc -p tsconfig.json --noEmit",
       "test": "vitest run",
       "test:watch": "vitest"
     }
     ```

3. Create `.gitignore` in repo root with exactly these lines (one per line, trailing newline):
   ```
   node_modules
   dist
   .crawl-session.json
   *.log
   .DS_Store
   ```

4. Create an empty entry file `src/index.ts` with exactly this content (one line, no trailing content):
   ```ts
   export {};
   ```
   This gives `tsc` something to compile so `npm run build` doesn't error on an empty rootDir. Plan 03 will replace it with real public-API re-exports.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit &amp;&amp; npx tsc -p tsconfig.json &amp;&amp; test -f dist/index.js &amp;&amp; test -f dist/index.d.ts &amp;&amp; grep -q '"rootDir": "./src"' tsconfig.json &amp;&amp; grep -q '"outDir": "./dist"' tsconfig.json &amp;&amp; grep -q '"main": "dist/index.js"' package.json &amp;&amp; grep -q 'node_modules' .gitignore &amp;&amp; grep -q '.crawl-session.json' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.json` contains the literal string `"rootDir": "./src"` (verifiable: `grep -q '"rootDir": "./src"' tsconfig.json`)
    - `tsconfig.json` contains the literal string `"outDir": "./dist"` (verifiable: `grep -q '"outDir": "./dist"' tsconfig.json`)
    - `tsconfig.json` still contains `"strict": true`, `"noUncheckedIndexedAccess": true`, `"exactOptionalPropertyTypes": true` (verifiable: three `grep -q` checks)
    - `tsconfig.json` no longer contains `"jsx":` (verifiable: `! grep -q '"jsx"' tsconfig.json`)
    - `package.json` scripts object contains `build`, `typecheck`, `test`, `test:watch` (verifiable: `node -e "const s=require('./package.json').scripts; ['build','typecheck','test','test:watch'].forEach(k=>{if(!s[k])throw new Error('missing '+k)})"`)
    - `package.json` `main` equals `dist/index.js` (verifiable: `node -e "if(require('./package.json').main!=='dist/index.js')process.exit(1)"`)
    - `package.json` `types` equals `dist/index.d.ts` (verifiable: `node -e "if(require('./package.json').types!=='dist/index.d.ts')process.exit(1)"`)
    - `.gitignore` contains `node_modules`, `dist`, `.crawl-session.json` (verifiable: three `grep -q` checks)
    - `src/index.ts` exists with content `export {};` (verifiable: `test -f src/index.ts &amp;&amp; grep -q 'export {};' src/index.ts`)
    - `npm run typecheck` exits 0 (verifiable: literal command)
    - `npm run build` produces `dist/index.js` and `dist/index.d.ts` (verifiable: `test -f dist/index.js &amp;&amp; test -f dist/index.d.ts`)
    - Emitted `dist/index.js` is CommonJS-compatible: contains `Object.defineProperty(exports` OR `"use strict"` header (verifiable: `grep -qE "(Object.defineProperty\\(exports|use strict)" dist/index.js`)
  </acceptance_criteria>
  <done>
    `tsc` builds CommonJS output into `dist/`, scripts are wired, `.gitignore` excludes node_modules / dist / .crawl-session.json.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create src/config directory placeholder and sanity-run vitest</name>
  <read_first>
    - /home/ubuntu/work/crawl.io/package.json
    - /home/ubuntu/work/crawl.io/tsconfig.json
  </read_first>
  <files>src/config/.gitkeep, vitest.config.ts</files>
  <action>
1. Create the directory `src/config/` and place an empty `.gitkeep` file inside so the directory is tracked by git. Exact file: `src/config/.gitkeep` with zero bytes.

2. Create `vitest.config.ts` at repo root with exactly this content:
   ```ts
   import { defineConfig } from 'vitest/config';

   export default defineConfig({
     test: {
       include: ['src/**/*.test.ts'],
       environment: 'node',
     },
   });
   ```

   Rationale: Phase 1 is pure-TS/Node; no DOM. Test files live next to source in `src/`. Vitest picks up `.test.ts` via the glob.

3. Run `npm test` once to confirm Vitest boots cleanly on an empty test set. Expected output includes "No test files found" OR "0 passed" — BOTH are acceptable exit codes depending on Vitest version; accept any exit code 0 or 1 with zero failures. If Vitest errors out with a config/import error, fix before concluding.

If Vitest returns exit code 1 solely due to "no tests found", add `passWithNoTests: true` under `test:` in `vitest.config.ts` so CI-style runs don't fail. Prefer `passWithNoTests: true` unconditionally — cheaper than deciding per-Vitest-version.
  </action>
  <verify>
    <automated>test -f src/config/.gitkeep &amp;&amp; test -f vitest.config.ts &amp;&amp; grep -q "src/\\*\\*/\\*.test.ts" vitest.config.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/.gitkeep` exists (verifiable: `test -f src/config/.gitkeep`)
    - `vitest.config.ts` exists at repo root (verifiable: `test -f vitest.config.ts`)
    - `vitest.config.ts` contains the glob `src/**/*.test.ts` (verifiable: `grep -q "src/\\*\\*/\\*.test.ts" vitest.config.ts`)
    - `vitest.config.ts` contains `passWithNoTests: true` (verifiable: `grep -q "passWithNoTests: true" vitest.config.ts`)
    - `npm test` exits 0 (verifiable: literal command)
    - `npx tsc -p tsconfig.json --noEmit` still exits 0 after all changes (verifiable: literal command)
  </acceptance_criteria>
  <done>
    Directory structure is ready, Vitest runs cleanly with zero tests, build pipeline verified end-to-end.
  </done>
</task>

</tasks>

<verification>
Toolchain smoke test, end-to-end:

1. `npm run typecheck` — must exit 0.
2. `npm run build` — must exit 0 and produce `dist/index.js` + `dist/index.d.ts`.
3. `npm test` — must exit 0 (zero tests is fine).
4. `cat package.json` — confirms deps, devDeps, scripts, main, types, files, type=commonjs.
5. `cat tsconfig.json` — confirms strict flags preserved, rootDir/outDir/lib/types added, jsx removed.
6. `cat .gitignore` — confirms node_modules, dist, .crawl-session.json listed.
</verification>

<success_criteria>
- `npm install` reproduces the dependency tree from `package-lock.json` on a fresh checkout
- `npm run build` emits CommonJS output loadable via `require()` from Node 20
- `npm test` runs Vitest and exits 0
- `src/config/` directory tracked in git via `.gitkeep`
- No code files (other than `src/index.ts` stub and config files) are created — Plan 02 owns types/schema/errors, Plan 03 owns parser
- `tsconfig.json` strict flags remain exactly as specified in CLAUDE.md
- `.gitignore` prevents accidental commit of `node_modules`, `dist`, and future `.crawl-session.json`
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-parser/01-01-SUMMARY.md` documenting: (a) resolved npm package versions for unified/remark-parse/yaml/zod/vitest/typescript, (b) any tsconfig changes applied, (c) confirmation that `build` + `test` scripts run green on empty project, (d) any npm warnings encountered during install.
</output>
